#include <stdio.h>

int main()
{
   int n;
   scanf("%d",&n);
   
   if (n>0) 
  {printf("no is positive");
  }
  else if (n<0)
  { printf("no is negative");}
  else 
  printf("zero");
  

    return 0;
}

